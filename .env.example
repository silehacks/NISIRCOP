# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nisircop_le
DB_USERNAME=your_postgres_username
DB_PASSWORD=your_postgres_password

# JWT Secret Key
# Use a long, random string for production. You can generate one with: openssl rand -base64 32
JWT_SECRET=a-very-secure-development-secret-key