# PostgreSQL Database Configuration
POSTGRES_USER=nisircop_user
POSTGRES_PASSWORD=nisircop_password
POSTGRES_DB=nisircop_db

# JWT Secret for Auth Service
# IMPORTANT: Use a long, random, and cryptographically secure string in production.
# You can generate one using: openssl rand -base64 32
JWT_SECRET=default-super-secret-key-that-must-be-changed