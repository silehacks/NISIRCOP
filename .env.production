# PRODUCTION Environment Configuration for NISIRCOP-LE
# CRITICAL: This file contains production settings - keep secure!

# JWT Configuration - MUST BE CHANGED IN PRODUCTION
# Generate with: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING_FROM_OPENSSL

# Default Admin Password - CHANGE IMMEDIATELY
DEFAULT_ADMIN_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# Database Configuration
DB_PATH=./data/nisircop.db

# Service Discovery
EUREKA_SERVER_URL=http://discovery-server:8761/eureka

# Application Environment
SPRING_PROFILES_ACTIVE=prod

# Security Settings
CORS_ALLOWED_ORIGINS=https://your-production-domain.com,https://admin.your-domain.com

# Logging Level
LOG_LEVEL=WARN

# Performance Settings
JVM_OPTS=-Xmx1024m -Xms512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# Health Check Settings
HEALTH_CHECK_TIMEOUT=30s
HEALTH_CHECK_INTERVAL=60s